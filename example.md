第一步：让它规划用例，不会生成算例或 Tcl。

```
使用 $cae-benchmark-test-suite 规划 CAE 对标测试：

- 待验证需求：验证自研求解器的 CELAS1 标量弹簧行为
- 参考软件及版本：MSC Nastran 2021
- 关键字：CELAS1、PELAS、SPC1、FORCE
- 所属模块：线性静力分析
- 测试用例范围：全部用例
- 测试用例类型：全部类型；其他类型为性能测试，验收目标为 10 万单元模型导入不超过 60 秒
- 求解范围：线性静力
- 单位制：N-mm-tonne-s
- 输出目录：C:\CAE_Test\CELAS1

请先生成测试规格、用例清单、禅道测试用例表和结果统计表，不生成原生算例或 Tcl。
```

第二步：从输出的用例清单中，每次确认一个编号，例如：

```
确认生成 T01 的 Nastran BDF 算例和对应 HyperMesh Tcl，并执行可用的导入检查。
```

完成后，再继续确认下一项：

```
确认生成 D01 的 Nastran BDF 输入和对应 HyperMesh Tcl。
```

即使选择“全部用例”，也需像这样逐项确认；每次只生成一个用例的算例和 Tcl