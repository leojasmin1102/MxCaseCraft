# 功能实现总结

## 已完成功能

### 1. 双模式支持

#### 贪婪模式 (Greed Mode)
- **命令**: `python main.py --mode greed` 或 `python main.py`
- **特点**: 
  - 读取input目录下所有Excel和Word文件
  - 将所有内容合并后直接发送给AI生成测试用例
  - 无需人工干预，全自动运行
  - 适合文档质量高、内容准确的场景

#### 理性模式 (Rationality Mode)
- **命令**: `python main.py --mode rationality`
- **特点**:
  - 从Excel文件按行提取需求（每行=一条需求）
  - 保留完整的行信息（所有列的内容）
  - 逐条显示需求，等待用户确认
  - 支持编辑、跳过、退出操作
  - 只使用确认的需求生成测试用例
  - 适合需要人工审核和筛选的场景

### 2. 文件读取功能
- 支持Excel文件 (`.xlsx`, `.xls`)
- 支持Word文件 (`.docx`, `.doc`)
- 自动读取input目录下的所有文档
- 显示读取进度和字符统计

### 3. AI生成功能
- 使用MiniMax API (MiniMax-M2.7模型)
- 实时进度显示（动态进度条）
- 自动重试机制（遇到529错误）
- 详细的日志输出

### 4. 输出功能
- **JSON格式**: `test_cases_output.json`
  - 标准JSON结构
  - 包含title、steps、expected字段
  
- **Excel格式**: `test_cases_output_YYYYMMDD_HHMMSS.xlsx`
  - 自动读取模板格式（17列）
  - 包含用例编号、标题、步骤、预期结果等
  - 自动格式化（表头样式、列宽、边框）
  - 时间戳文件名避免覆盖

- **需求记录** (仅理性模式): `confirmed_requirements.json`
  - 保存用户确认的需求列表
  - 包含需求ID、来源、内容等信息

### 5. 需求提取功能 (理性模式专用)
- 按Excel行提取需求
- 每行作为一条独立需求
- 保留所有列信息
- 显示来源（工作表名、行号）
- 支持交互式确认和编辑

## 项目结构

```
MxCaseCraft/
├── input/                              # 输入文档目录
│   └── 任务.xlsx                       # Excel需求文档
├── outputformat/                       # Excel模板目录
│   └── MxSim.Mechanical-通用-GUI-V3.0.0测试用例.xlsx
├── minimax_client.py                   # MiniMax API客户端
├── test_case_generator.py              # 测试用例生成器
├── file_reader.py                      # 文件读取模块
├── excel_writer.py                     # Excel输出模块
├── requirement_manager.py              # 需求管理模块
├── main.py                             # 主程序入口
├── test_excel_extraction.py            # Excel提取测试脚本
├── test_rationality.py                 # 理性模式演示脚本
├── requirements.txt                    # 依赖包
├── .env                                # 环境变量配置
├── .env.example                        # 环境变量示例
└── README.md                           # 使用说明

输出文件:
├── test_cases_output.json              # JSON格式测试用例
├── test_cases_output_YYYYMMDD_HHMMSS.xlsx  # Excel格式测试用例
└── confirmed_requirements.json         # 确认的需求列表（理性模式）
```

## 使用示例

### 贪婪模式
```bash
# 运行贪婪模式
python main.py --mode greed

# 输出示例
[主程序] 运行模式: greed
[主程序] 读取Excel文件: 任务.xlsx
[主程序] 文件读取完成，总长度: 3003 字符
[主程序] === 贪婪模式 ===
[API] 正在调用MiniMax API...
[API] 等待中... (已等待 30 秒)
[API] 响应接收完成 (耗时 63 秒)
[生成器] 成功生成 10 个测试用例
[主程序] 完成！共生成 10 个测试用例
```

### 理性模式
```bash
# 运行理性模式
python main.py --mode rationality

# 输出示例
[主程序] 运行模式: rationality
[需求管理] 从Excel文件提取需求: input\任务.xlsx
[需求管理] 提取到 10 条需求

================================================================================
需求 #1
来源: 工作表 [Sheet1] 第 2 行
================================================================================
功能大类: MxSim.Mech
功能子类: CAD
当前基线: 当前版本不支持草图模式
功能名称: 当前版本不支持草图创建功能
...
================================================================================

请选择操作 [y/e/s/q] (默认: y): y
✓ 需求 #1 已确认

[继续显示下一条需求...]
```

## 技术特点

1. **进度可视化**: 实时显示API调用进度，避免用户误以为程序卡死
2. **错误处理**: 自动重试机制，处理API服务器过载（529错误）
3. **中文支持**: 正确处理Windows控制台中文显示
4. **模板化输出**: 自动读取Excel模板格式，保持输出一致性
5. **交互式设计**: 理性模式提供友好的交互界面
6. **灵活配置**: 通过命令行参数切换不同模式

## 依赖包

```
openai>=1.0.0
python-dotenv>=1.0.0
openpyxl
python-docx
```

## 配置要求

需要在 `.env` 文件中配置：
```
OPENAI_API_KEY=your_api_key_here
OPENAI_BASE_URL=https://api.minimax.chat/v1
```
